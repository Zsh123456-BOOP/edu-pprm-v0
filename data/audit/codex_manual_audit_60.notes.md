# Codex Manual Proxy Audit Notes

These labels are `codex_manual_proxy`, not `heuristic_proxy` and not human labels.

The pass used only `audit_60_blind.jsonl`, guideline, and taxonomy. Hidden expected labels and synthetic type were not used.

## Distribution

```json
{
  "ask_to_recompute_local_expression": 14,
  "no_intervention_needed": 22,
  "insufficient_information": 12,
  "ask_to_reinterpret_given_quantity": 12,
  "ask_to_check_unit_conversion": 2,
  "ask_to_check_operation_or_formula": 1,
  "ask_to_justify_inference": 5
}
```
